import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: '#0052cc',
    },
    secondary: {
      main: '#edf2ff',
    },
    background: {
      default: '#969090',
      paper: '#fbf7f7',
    },
  },
});

export default theme;